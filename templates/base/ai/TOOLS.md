# Tooling conventions

- Prefer repo-native tooling and configs (`pyproject.toml`, linters, tests).
- Keep automation in `scripts/` and CI in `.github/workflows/`.
- Run format/lint via pre-commit when available.

